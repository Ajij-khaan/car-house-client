import React from 'react';
import { Alert } from 'react-bootstrap';

const Pay = () => {
    return (
        <div>
            <Alert variant="info">Payment System Coming Soon</Alert>
        </div>
    );
};

export default Pay;